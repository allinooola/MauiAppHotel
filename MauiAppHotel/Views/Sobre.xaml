<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiAppHotel.Views.Sobre"
             Title="Sobre"
             NavigationPage.HasNavigationBar="True"
             BackgroundColor="{StaticResource Gray100}">

    <VerticalStackLayout Padding="24" Spacing="0" VerticalOptions="Center">

        <!-- Ícone -->
        <Frame WidthRequest="72"
               HeightRequest="72"
               CornerRadius="18"
               Padding="0"
               HasShadow="False"
               BackgroundColor="#E1F5EE"
               BorderColor="Transparent"
               HorizontalOptions="Center"
               Margin="0,0,0,16">
            <Label Text="🏨"
                   FontSize="34"
                   HorizontalOptions="Center"
                   VerticalOptions="Center"/>
        </Frame>

        <!-- Nome e descrição -->
        <Label Text="MauiAppHotel"
               FontFamily="Kalam"
               FontSize="32"
               TextColor="{StaticResource Primary}"
               HorizontalOptions="Center"
               Margin="0,0,0,6"/>

        <Label Text="Projeto desenvolvido para a atividade de .NET MAUI."
               FontSize="14"
               TextColor="{StaticResource Gray500}"
               HorizontalOptions="Center"
               HorizontalTextAlignment="Center"
               Margin="0,0,0,24"/>

        <BoxView HeightRequest="0.5"
                 Color="{StaticResource Gray200}"
                 Margin="0,0,0,16"/>

        <!-- Card de infos -->
        <Frame Padding="0"
               CornerRadius="14"
               HasShadow="False"
               BorderColor="{StaticResource Gray200}"
               BackgroundColor="{StaticResource White}">
            <VerticalStackLayout Spacing="0">

                <Grid ColumnDefinitions="Auto, *, Auto"
                      Padding="14,12"
                      ColumnSpacing="10">
                    <Label Grid.Column="0" Text="👩‍💻" FontSize="16" VerticalOptions="Center"/>
                    <Label Grid.Column="1" Text="Desenvolvedora" FontSize="13" TextColor="{StaticResource Gray500}" VerticalOptions="Center"/>
                    <Label Grid.Column="2" Text="Lara Vitória" FontSize="13" FontAttributes="Bold" TextColor="{StaticResource Gray900}" VerticalOptions="Center"/>
                </Grid>

                <BoxView HeightRequest="0.5" Color="{StaticResource Gray200}" Margin="14,0"/>

                <Grid ColumnDefinitions="Auto, *, Auto"
                      Padding="14,12"
                      ColumnSpacing="10">
                    <Label Grid.Column="0" Text="📅" FontSize="16" VerticalOptions="Center"/>
                    <Label Grid.Column="1" Text="Ano" FontSize="13" TextColor="{StaticResource Gray500}" VerticalOptions="Center"/>
                    <Label Grid.Column="2" Text="2026" FontSize="13" FontAttributes="Bold" TextColor="{StaticResource Gray900}" VerticalOptions="Center"/>
                </Grid>

                <BoxView HeightRequest="0.5" Color="{StaticResource Gray200}" Margin="14,0"/>

                <Grid ColumnDefinitions="Auto, *, Auto"
                      Padding="14,12"
                      ColumnSpacing="10">
                    <Label Grid.Column="0" Text="📱" FontSize="16" VerticalOptions="Center"/>
                    <Label Grid.Column="1" Text="Aplicativo" FontSize="13" TextColor="{StaticResource Gray500}" VerticalOptions="Center"/>
                    <Label Grid.Column="2" Text="MauiAppHotel" FontSize="13" FontAttributes="Bold" TextColor="{StaticResource Gray900}" VerticalOptions="Center"/>
                </Grid>

            </VerticalStackLayout>
        </Frame>

    </VerticalStackLayout>

</ContentPage> <?teste?>